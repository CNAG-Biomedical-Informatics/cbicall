{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cbicall:workflows.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["workflows"],
  "properties": {
    "workflows": {
      "type": "object",
      "additionalProperties": false,
      "required": ["bash"],
      "properties": {
        "bash": { "$ref": "#/$defs/engine" },
        "snakemake": { "$ref": "#/$defs/engine" },
        "nextflow": { "$ref": "#/$defs/engine" }
      }
    }
  },
  "$defs": {
    "pathString": {
      "type": "string",
      "minLength": 1
    },
    "engine": {
      "type": "object",
      "additionalProperties": false,
      "required": ["base_dir", "versions"],
      "properties": {
        "base_dir": { "$ref": "#/$defs/pathString" },
        "versions": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/version" }
        }
      }
    },
    "version": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pipelines"],
      "properties": {
        "common": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "env": { "$ref": "#/$defs/pathString" },
            "config": { "$ref": "#/$defs/pathString" },
            "coverage": { "$ref": "#/$defs/pathString" },
            "jaccard": { "$ref": "#/$defs/pathString" },
            "vcf2sex": { "$ref": "#/$defs/pathString" }
          }
        },
        "pipelines": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/pipelineModes" }
        }
      }
    },
    "pipelineModes": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "single": { "$ref": "#/$defs/pathString" },
        "cohort": { "$ref": "#/$defs/pathString" }
      },
      "required": ["single"]
    }
  }
}
